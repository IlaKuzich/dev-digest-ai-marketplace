# example-plugin

Starter plugin used as a template for new entries in this marketplace.

## Structure

- `.claude-plugin/plugin.json` — plugin manifest (required)
- `commands/` — slash commands (flat `.md` files)
- `skills/` — auto-discovered skills

Copy this directory as a starting point for a new plugin, then update `plugin.json` and remove this note.
