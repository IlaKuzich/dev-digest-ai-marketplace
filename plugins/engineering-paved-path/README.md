# engineering-paved-path (placeholder)

Not yet populated. Intended to bundle the team's supported engineering conventions as
Claude Code skills (backend/frontend/full-stack best practices, PR self-review gate)
so they can be installed independently of the DevDigest product repo.

Source material to migrate lives in a separate private repository and has not been
copied here yet — this directory only reserves the shape (`.claude-plugin/plugin.json`)
so the marketplace structure validates. See [../../docs/PLUGIN-GUIDELINES.md](../../docs/PLUGIN-GUIDELINES.md)
for what a real plugin submission must contain before this placeholder is replaced.
