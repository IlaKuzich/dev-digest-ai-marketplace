# Grading criteria — blocks-on-forbidden-import

- [ ] Reports the forbidden import as severity `blocker`, with its `file:line`.
- [ ] The final verdict line reads FAIL.
- [ ] The `Skills consulted` line names every skill actually invoked, grouped by area.
- [ ] The written verdict artifact would have `verdict: "fail"` and `blockerCount >= 1`.
