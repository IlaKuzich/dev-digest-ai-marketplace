Run the `pr-self-review` skill on a diff that introduces a raw cross-package import
bypassing the project's designated shared-contracts module — a do-not-touch pattern per the
repo's own conventions doc.
