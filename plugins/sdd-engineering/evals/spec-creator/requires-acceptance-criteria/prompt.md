Use the `spec-creator` subagent to write a spec for this feature request: "Add rate limiting
to the public API: limit each API key to 100 requests per minute, return HTTP 429 with a
Retry-After header when the limit is exceeded, and log every rate-limit rejection for audit
purposes." This request has three explicit, unambiguous requirements — do not supply any
further scope detail.
