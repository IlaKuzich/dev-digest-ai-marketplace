.claude/settings.local.json

# site — generated build-time data + build output (regenerated by CI on every push)
site/node_modules/
site/dist/
site/public/data/
site/public/feed.xml

# plugin eval runs — regenerated per CI run, not committed
evals/results/
